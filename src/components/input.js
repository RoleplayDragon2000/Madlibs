import React from 'react';

const Input = (title) => {
    return (
        <div className="input">
            <imput/>
            <label>{title}</label>
        </div>   
    )
}

export default input